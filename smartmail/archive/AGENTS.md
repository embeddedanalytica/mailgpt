## Archive Rules

- Everything under this `archive/` tree is historical or speculative material.
- Do not use files here as implementation authority, planning authority, or current-state context.
- LLM agents should ignore this tree by default unless the user explicitly asks for historical reference.
- When a file here conflicts with active docs outside `archive/`, the non-archived doc wins.
